{
  "name": "imap-server",
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "preview": "node .output/server/index.mjs",
    "format": "oxfmt && oxfmt --check"
  },
  "devDependencies": {
    "h3": "^1.15.4",
    "nitropack": "^2.12.4",
    "oxfmt": "^0.21.0"
  }
}
